[advisories]
# RUSTSEC-2023-0071 (rsa via sqlx-mysql) is not in the active build graph.
# We only enable postgres for sqlx, and sqlx-mysql is unused for current targets.
#
# RUSTSEC-2025-0134 (rustls-pemfile) is dev-only via testcontainers in #[cfg(test)].
ignore = [
    "RUSTSEC-2023-0071",
    "RUSTSEC-2025-0134",
]
